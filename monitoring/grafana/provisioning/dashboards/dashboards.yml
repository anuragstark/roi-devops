apiVersion: 1

providers:
  - name: ROI Platform
    orgId: 1
    folder: ROI Platform
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
